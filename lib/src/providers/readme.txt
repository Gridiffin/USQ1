Purpose:
Manages the state of the app (e.g., user login status, product data, or app-wide settings).
Description:
Providers act as "managers" for data that can change in the app. 
They notify screens when data changes so the UI updates automatically.