Purpose:
Contains logic to interact with APIs, databases, or any external sources of data.

Description:
This folder handles backend tasks like fetching product details, user authentication, or sending chat messages. 
It keeps all data-fetching logic separate from the UI.